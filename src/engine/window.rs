
use wgpu::{Surface, Device, Queue, Config, Window}

struct WindowState {
    surface: Surface,
    device: Device,
    queue: Queue,
    config: SurfaceConfiguration,
    pub size: PhysicalSize<u32>,
    window: Window,
}

impl WindowState {


    pub fn create_render_pipeline() -> {
        
    }
}
